name: tmuxS
root: ~/

windows:
  - main :
      layout: f471,135x33,0,0[135x27,0,0{67x27,0,0,0,67x27,68,0,1},135x5,0,28{41x5,0,28,2,93x5,42,28,3}]
      panes:
        - logs:
          - sleep 0.1 && clear && screenfetch -c 255,41 -n && yaourt -Syua
        - logs:
          - clear && sh /home/armin/.bin/vimSession.sh
        - cheat:
          - ruby ~/.bin/cheatsheet.rb
        - stats:
          - journalctl -f -b-0
            
  # - ArchARMv7 :
      # layout: 6a46,284x76,0,0{142x76,0,0[142x62,0,0,0,142x13,0,63,14],141x76,143,0[141x62,143,0,3,141x13,143,63,5]}
      # panes:
        # - logs:
          # - ssh ArchARMv7
          # - sleep 0.1 && clear && screenfetch -c 255,41 -n
        # - stats:
          # - ssh ArchARMv7
          # - yaourt -Syua
        # - logs:
          # - ssh ArchARMv7
          # - 'sh .bin/vimSession.sh'
        # - stats:
          # - ssh ArchARMv7
          # - journalctl -f
